{
  "name": "website",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc && node run-conversion.js && tsc --watch",
    "build": "tsc && node run-conversion.js"
  },
  "devDependencies": {
    "@types/jsdom": "^27.0.0",
    "@types/node": "^25.2.0",
    "jsdom": "^28.0.0",
    "typescript": "^5.7.3"
  }
}
